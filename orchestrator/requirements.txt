# =============================================================================
# PPTX POC - Orchestrator Service Dependencies
# =============================================================================
# FastAPI backend with async Ollama integration
# =============================================================================

# Web framework
fastapi>=0.109.0,<1.0.0

# ASGI server
uvicorn[standard]>=0.27.0,<1.0.0

# Async HTTP client for Ollama communication
httpx>=0.26.0,<1.0.0

# Data validation (included with FastAPI, but pinned for clarity)
pydantic>=2.5.0,<3.0.0

# Settings management
pydantic-settings>=2.1.0,<3.0.0

# Retry logic for Ollama requests
tenacity>=8.2.0,<9.0.0

# YAML parsing for prompt configuration
pyyaml>=6.0.0,<7.0.0
